import React from 'react';
import { Button } from 'react-bootstrap';

function CurrentServiceViewer() {
	return (
		<div className="Reg-form">
			<Button variant="primary" type="submit" className="Reg-button">
				Stop Service
			</Button>
		</div>
    )
}

export default CurrentServiceViewer;
